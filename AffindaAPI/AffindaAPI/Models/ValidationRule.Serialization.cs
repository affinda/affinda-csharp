// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

// <auto-generated/>

#nullable disable

using System.Collections.Generic;
using System.Text.Json;
using Azure.Core;

namespace Affinda.API.Models
{
    public partial class ValidationRule : IUtf8JsonSerializable
    {
        void IUtf8JsonSerializable.Write(Utf8JsonWriter writer)
        {
            writer.WriteStartObject();
            writer.WritePropertyName("slug");
            writer.WriteStringValue(Slug);
            writer.WritePropertyName("dataPoints");
            writer.WriteStartArray();
            foreach (var item in DataPoints)
            {
                writer.WriteStringValue(item);
            }
            writer.WriteEndArray();
            writer.WriteEndObject();
        }

        internal static ValidationRule DeserializeValidationRule(JsonElement element)
        {
            string slug = default;
            IList<string> dataPoints = default;
            foreach (var property in element.EnumerateObject())
            {
                if (property.NameEquals("slug"))
                {
                    slug = property.Value.GetString();
                    continue;
                }
                if (property.NameEquals("dataPoints"))
                {
                    List<string> array = new List<string>();
                    foreach (var item in property.Value.EnumerateArray())
                    {
                        array.Add(item.GetString());
                    }
                    dataPoints = array;
                    continue;
                }
            }
            return new ValidationRule(slug, dataPoints);
        }
    }
}
